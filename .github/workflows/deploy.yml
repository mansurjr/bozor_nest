name: Deploy MyRent Backend

on:
  push:
    branches: [ main ]

  workflow_dispatch:
    inputs:
      api:
        description: "PM2 app name (optional)"
        required: false
        default: ""

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /root/backend
            ./deploy.sh "${{ github.event.inputs.api }}"
  telegram:
    needs: deploy
    runs-on: ubuntu-latest

    steps:
      - name: Send Telegram Notification
        uses: appleboy/telegram-action@v0.1.5
        with:
          to: "8478335566"
          token: "8326579724:AAHRsNAluyIFVVf3siXwwo2B2GZqUYWWa90"
          message: "🚀 MyRent Backend has been deployed successfully to the server."